create table Employee (
	EMPLOYEE_ID BIGINT,
	EMPLOYEE_USER_NAME VARCHAR(50),
	EMPLOYEE_FIRST_NAME VARCHAR(50),
	EMPLOYEE_LAST_NAME VARCHAR(50),
	EMPLOYEE_DEPARTMENT VARCHAR(5),
	EMPLOYEE_ROLE VARCHAR(17),
	EMPLOYEE_ADDRESS VARCHAR(50)
);